import { SafeAreaView } from 'react-native';
import ViewText from '../Components/ViewText';

const HomeScreen = () => {
  return (
    <SafeAreaView>
      <ViewText showText="Profile Screen" />
    </SafeAreaView>
  );
};

export default HomeScreen;

